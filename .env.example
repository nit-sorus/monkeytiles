# Node.js server configuration
PORT=3001
NODE_ENV=development

# Optional: enables persistent rooms and lifetime scores.
# Insert a local development URI in .env; never commit the real credential.
MONGODB_URI=